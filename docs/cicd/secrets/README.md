# Secrets

This folder is gitignored. Place real secret values here manually — never let an AI agent read or write to this folder.

Expected files (as needed for CI/CD):
- `keystore-base64.txt` — base64-encoded `rutambhapps.jks`
- `keystore-password.txt`
- `key-alias.txt`
- `key-password.txt`
- `play-store-service-account.json`
